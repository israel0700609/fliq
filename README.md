# Fliq - MovieVibe

This project is a mobile application that provides movie recommendations based on the user's mood.

## Project Structure

The project is divided into two main parts:

- `client`: A React Native application built with Expo.
- `server`: Backend logic using Supabase Edge Functions.


